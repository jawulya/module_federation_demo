import React from 'react';
import {Link} from "react-router-dom";

export const Checkout = () => {
    return (
        <div>
            <h1>CHECKOUT</h1>
            <Link to="/">Navigate to CART</Link>
        </div>
    );
};